{
  "name": "delhi-line-hotel",
  "version": "1.0.0",
  "description": "Delhi Line Hotel Full Stack App",
  "scripts": {
    "install": "npm --prefix server install && npm --prefix client install --include=dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
